# Use CPU-only PyTorch wheels to avoid downloading CUDA/NVIDIA packages during build
# Use PyPI as the primary index and add PyTorch CPU wheels as an extra index
--extra-index-url https://download.pytorch.org/whl/cpu
flask
torch==2.10.0
torchvision==0.25.0
ultralytics
opencv-python
numpy
gunicorn
pillow
facenet-pytorch
